apiVersion: v1
kind: ConfigMap
metadata:
  name: ws-configmap
data:
  nginx.conf: |
    server {
      listen 80;
      server_name _;
      location / {
        proxy_pass http://app-svc:8000;
      }
    }